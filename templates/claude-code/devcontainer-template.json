{
    "id": "claude-code",
    "version": "1.0.0",
    "name": "Claude Code",
    "description": "A devcontainer with Claude Code pre-installed and host authentication configured",
    "options": {
        "imageVariant": {
            "type": "string",
            "description": "Base image variant",
            "proposals": ["ubuntu-24.04", "ubuntu-22.04", "debian-12"],
            "default": "ubuntu-24.04"
        }
    }
}
